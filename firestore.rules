rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SOLO para desarrollo local (emulador):
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
