export default function TransferForm({
  amount,
  paymentData,
  setOpenModal
}: {
  amount: any,
  paymentData: any,
  setOpenModal: any
}) {

  return (
    <div>
      TransferForm
    </div>
  )
}